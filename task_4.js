//Day 4
/* let num = prompt('Enter number');
switch (true) {
  case num > 0:
    console.log('Number is positive');
    break;
  case num == 0:
    console.log('Numbers is zero');
    break;
  case num < 0:
    console.log('Number is negative');
    break;
  default:
    console.log('Entered value was not a number');
} */
/*
let userAge = prompt ("Enter your age:");
let underAge = 18 - userAge;
if (userAge >= 18){
    console.log (`You are ${userAge} and you are old enough to drive`)
  } else if (userAge < 18) {
    console.log (`You are ${userAge} and will be permitted to drive in ${underAge} years.`)};*/
/*const myAge = 26;
let yourAge = prompt ("Enter your age:"),
    higherAgeDifference = yourAge - myAge,
    lowerAgeDifference = myAge - yourAge;

if (yourAge > myAge) {
    console.log (`You are ${higherAgeDifference} years older than I am`) 
  } else if (myAge == yourAge) {
    console.log ("Cheers, we are of the same age")
   } else {
    console.log (`I am ${lowerAgeDifference} years older than you`)}*/

/* let a = 4
 let b = 3
  if (a > b) {
    console.log (`${a} is greater than ${b}`)
   } else {
    console.log (`${a} is greater than ${b}`)};

let a = 4,
    b= 3;
a > b
    ? console.log (`${a} is greater than ${b}`)
    : console.log  (`${a} is greater than ${b}`); */
// Even numbers are divisible by 2 and the remainder is zero. How do you check, if a number is even or not using JavaScript?

/*let evenNum = prompt ("Enter number:"),
    remainder = evenNum % 2;
if (remainder == 0){
    console.log (`${evenNum} is an even number`)
  } else {
    console.log (`${evenNum} is a odd number`)
    }
let GNS = +prompt ("Enter your GNS score:"),
    ABE = +prompt ("Enter your ABE score:"),
    FST = +prompt ("Enter your FST score:"),
    CVE = +prompt ("Enter your CVE score:");
switch (GNS) {
  case num > 0:
    console.log('Number is positive');
    break;
  case num == 0:
    console.log('Numbers is zero');
    break;
  case num < 0:
    console.log('Number is negative');
    break;
  default:
    console.log('Entered value was not a number');
}*/
/* let GNS = prompt('Enter GNS score:');
switch (true) {
   case GNS <= 0:
    console.log('Woeful');
    break;
  case GNS <= 49:
    console.log('F');
    break;
   case GNS <= 59:
    console.log('D');
    break;
   case GNS <= 69:
    console.log('C');
    break;
   case GNS <= 79:
    console.log('B');
    break;
   case GNS <= 100:
    console.log('A');
    break;
    case GNS > 100:
    console.log("Score shouldn't be greater than 100");
    break;
  default:
    console.log('Entered value was not a number');
}
*/
/*
let userInputMonth = prompt ("Enter Month of the year:");
let month = userInputMonth.toLowerCase();
  
 if (month === "september" || month === "october"|| month === "november") {
     console.log ("The season is Autumn")
    } else if (month === "december" || month === "january" || month === "february") {
        console.log ("The season is Winter.")
    } else if (month === "march" || month === "april" || month === "may") {
        console.log ("The season is Spring")
    } else if (month === "june" || month === "july" || month === "august") {
        console.log ("The season is Summer")
    } else {
        console.log ("Enter a valid month or check your spelling")
    };
*/

   /* let userInputDay = prompt ("Enter day of the week:");
    let day = userInputDay.toLowerCase();
      
     if (day === "monday" || day === "tuesday"|| day === "wednesday" || day === "thursday" || day === "friday") {
         console.log (`${day} is a working day`)
        } else if (day === "saturday" || day === "sunday") {
            console.log (`${day} is a weekend`)
        } else {
            console.log ("Enter a valid day of the week or check your spelling")
        } */
      /*  let userInputMonth = prompt ("Enter month:");
        let month = userInputMonth.toLowerCase();
    if (month === "april" || month === "june"|| month === "september" || month === "november") {
        console.log (`${month} has 30 days`)
    } else if (month === "january" || month === "march"|| month === "may" || month === "june" ||
            month === "july" || month === "august"|| month === "october" || month === "december") {
        console.log (`${month} has 31 days`)
    } else if (month === "february") {
        console.log (`${month} has 28 days`) 
        } else {
        console.log ("Enter a valid day of the week or check your spelling")
    } */


